# builtin.remote-ssh

Mock Remote SSH extension for Lvce Editor.

Version `0.1.0` deliberately provides a session-only in-memory file system. It
does not make SSH connections, authenticate, install a remote server, or persist
hosts.

## Contributing

```sh
git clone git@github.com:lvce-editor/remote-ssh.git
cd remote-ssh
npm ci
npm test
```
